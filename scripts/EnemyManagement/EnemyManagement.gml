/// @description Enemy Management Scripts
/// GMLive setup
//if (live_call()) return live_result; // No parameters
//if (live_call(argument0, argument1)) return live_result; // Two parameters

function scrFlingEnemyInstance(_enemy) {
	if (live_call()) return live_result;
	direction = choose(45, 45, 80, 110, 135, 135)
	speed = deadHitSpeed
}

